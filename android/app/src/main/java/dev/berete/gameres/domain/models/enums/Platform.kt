package dev.berete.gameres.domain.models.enums

/**
 * A Game platform e.g XBOX
 */
enum class Platform {
    PLAYSTATION,
    XBOX,
    PC,
    NINTENDO,
    ANDROID,
    APPLE, // MACOS && IOS
    LINUX,
}